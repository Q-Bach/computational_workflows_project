# Use an official Python runtime as a parent image
FROM python:3.10

# Install necessary Python packages
RUN pip install -r requirements.txt

# Define the default command to run your script
CMD ["python", "generate_counts_plot.py"]
